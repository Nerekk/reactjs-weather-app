import { Autocomplete, Box} from "@mui/material";
import { CitySearch } from "./CitySearch";

export const Locations = () => {
    return (
        <Box>
            <CitySearch/>
        </Box>
    );
}